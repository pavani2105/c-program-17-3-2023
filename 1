#include<stdio.h>
main()
{
	int fact(int x);
	int a,r;
	printf("enter a number:");
	scanf("%d",&a);
	r=fact(a);
	printf("%d",r);
	
}
int fact(int x)
{
	if(x==1)
	 return 1;
	else
	 return (x*fact(x-1));
}
